# ZEP-0020：ZyenLang 0.3 核心語言模型

[English](ZEP-0020-v0.3-core-language.md) | **繁體中文**

| 欄位 | 內容 |
|---|---|
| **ZEP** | 0020 |
| **標題** | ZyenLang 0.3 核心語言模型 |
| **作者** | zuenchen、OpenAI Codex |
| **狀態** | Final |
| **類型** | Standards |
| **建立日期** | 2026-09-18 |
| **取代** | ZEP-0006、ZEP-0010、ZEP-0011、ZEP-0013、ZEP-0014、ZEP-0015、ZEP-0016、ZEP-0018 |
| **參考實作** | `Ryan-2013/zyenlang` 的 `v0.3.0` tag |

## 摘要

ZyenLang 0.3 是刻意不相容的語言修訂。它把資料與身份分開，以受檢查的區域
reference 取代一般程式裡的裸 pointer，讓所有 managed value 使用同一套 cleanup
模型，並讓名稱路徑只剩一套無歧義規則。第一個 backend 仍輸出可攜式 C11，
但完成型別檢查的程式以不依賴 C AST 的 Typed IR 表示。

0.3 不提供 0.2 相容模式。舊寫法必須得到 migration diagnostic；有對應新寫法時，
錯誤訊息應直接顯示。

## Module 與成員存取

每個 `.zy` 檔案就是一個 module：

```zy
import std::io as io
import crate::model as model
import utils::text as text
```

import root 只允許 `std::`、`crate::` 與直接 dependency alias。
`crate::net::http` 對應 `src/net/http.zy`。字串 import、`<std/...>`、wildcard、
selective import、re-export、路徑穿越與隱式 alias 都不合法。

`::` 解析 module、type 與 static method；`.` 只解析 instance field 或 class
method。因此 `io::print()`、`model::Point`、`File::open()`、`file.close()`、
`point.x` 合法，而 `io.print()`、`model.Point` 必須報錯。

## Struct 與 class

`struct` 是純資料值型別，body 只能放 field，禁止 `fn`、`mut fn`、
`static fn`、`init` 與 `deinit`。

```zy
public struct Point {
    public x: i32
    public y: i32
    private label: str = "point"
}
```

field 預設 private。宣告它的 module 可存取 private field；外部只可存取 public
field。建構使用 field literal，省略欄位採宣告 default 或型別零值。純 Copy
struct 直接複製；含 managed value 的 struct 由編譯器產生遞迴 copy/cleanup。
遞迴 by-value layout 在編譯期拒絕。資料行為使用 module function 表達。

有身份與封裝需求時使用 atomic ARC class：

```zy
public class Cache<T> {
    private value: T

    public init(value: T) {
        this.value = value
    }

    public fn get() T {
        return CLONE__(this.value)
    }

    public mut fn set(value: T) void {
        this.value = value
    }

    public static fn identity(value: T) T {
        return value
    }

    deinit {
    }
}
```

`Cache<i32>(10)` 呼叫 `init`。泛型 class 必須明確提供 type argument。
readonly `fn` 不能修改 `this` 或呼叫 mut method；`mut fn` 才可以。
`static fn` 沒有 `this`。`deinit` 無參數、不能 throws，只在最後一個 ARC owner
釋放後執行。0.3 沒有繼承、隱式 subtype、variance、weak reference 或 cycle
collector。

泛型函式與 class 採 reachable monomorphization。每個實際 specialization 的運算
都必須能通過型別檢查。0.3 不提供 generic constraint 或 method-level 額外泛型。

## Value、string 與 List

數值型別是 `i8/i16/i32/i64`、`u8/u16/u32/u64`、`isize/usize`、`f32/f64`。
混合運算提升至可共同表示的型別。其他核心型別包含 `bool`、`void`、`Error`、
tuple 與 `T | null`。成功的 null check 會在分支內把 optional 縮窄成 `T`。

`str` 是不可變的 ARC UTF-8 字串。字元長度、索引與切片以 Unicode scalar 為
單位；byte length 使用獨立操作。

`List<T>` 是強型別 managed container。複製後先共用 atomic ARC backing buffer，
第一次修改才 copy-on-write detach。managed element 會遞迴 retain/release，List
可放入 struct/class field。標準 intrinsic 是 `LIST_LEN__()`、`LIST_GET__()`、
`LIST_SET__()`、`LIST_PUSH__()`、`LIST_POP__()`、`LIST_CLEAR__()`。

所有 compiler intrinsic 統一採用含括號的 `NAME__()`。
`GET_ARGS__()` 回傳完整 process argument vector：索引零是程式路徑，使用者參數
從索引一開始。`GET_EXE__()` 是相同的程式路徑值。`FILE__()` 是該 expression
所在 source module 的絕對路徑，不是執行檔路徑。

## 函式值與 closure

函式值型別寫成 `fn(P...) R`，可以當參數、回傳值、managed field、closure、
callback，也能從任意有函式型別的 expression 呼叫。`pick("add")(20, 22)` 這類
連續呼叫的 callee 與每個參數都只求值一次。

具名函式、closure 與 native callback 共用邏輯表示：

```text
{ call, env, owner, signature }
```

具名函式沒有 environment owner。closure 以 readonly snapshot capture，環境由
atomic ARC 管理；captured managed value 會遞迴 retain/release。函式簽章必須
完全相同，不支援 function-to-raw-pointer cast。

## Reference 與 ownership

安全 reference 是區域 borrow：

```zy
let value: i32 = 10
let read: &i32 = &value
let copied: i32 = CLONE_REF__(read)
let write: &mut i32 = &mut value
REF_SET__(write, 20)
```

可同時存在多個 `&T`；`&mut T` 必須唯一，存在期間排除其他 borrow 與 owner
存取。reference 不可 null、不可巢狀。0.3 只允許 reference 作為 local 與 function
parameter，禁止回傳、capture、存入 aggregate 或跨 thread 保存。編譯器必須拒絕
可判定的懸空 reference 與 alias 違規。

loan 以 place 追蹤，包含 field 與 List element projection；一般在 reference 最後一個
statement-level 使用後結束。完整的 `if`/`while` statement 是保守 loan 區域。
readonly reference 可複製；mutable reference 為 linear，但 `&mut T` 可暫時視為
`&T`，也可直接傳給另一個函式。

class 與 List receiver 會自動解 reference，普通 field access 不會。class reference
是 shared ARC identity 上的 capability：`&Class` 只能呼叫 readonly method；
`&mut Class` 也能呼叫 mutating method 或替換自己的 handle slot。這不是全域唯一性
保證，其他普通 class alias 仍可能修改同一個 instance。

List 參數模式有三種明確語意：

- `List<T>` 是 COW value，mutation 只修改該 parameter value；
- `&List<T>` 是零拷貝 readonly view；
- `&mut List<T>` 會把結構性 mutation 寫回 caller 的 List handle。

`&list[index]` 與 `&mut list[index]` 是會 throws 的 bounds-checked element borrow。
其內部 `{data, pin}` 表示可 retain List backing ARC owner，讓 readonly element
reference 在 COW detach 或 reallocation 後仍有效。pin 只擁有 storage 生命期，不
擁有 pointee value。mutable element borrow 只允許 owned local List 與 value-struct
field，不可穿越 class 或 reference alias。可以直接 borrow class/struct field；若路徑
穿過巢狀 class handle，必須先把該 handle 綁成 local。

`CLONE__(value)` 執行該型別的一般 clone：ARC retain、struct 遞迴複製、List
保留 COW buffer。assignment 與 return 使用相同的 typed ownership operation。

`DROP__(local)` 提前執行正常 cleanup，並把 binding 標成未初始化。重新賦值前
不可讀取、borrow 或再次 drop。primitive、reference 與無資源 Copy struct 不可
明確 drop。`FREE__()`、`SKIP__()` 已移除。

`defer` 只登記不會 throws 的 function/class-method call。receiver 與參數在登記
時各求值一次，於一般離開 block、`return`、`break`、`continue`、catch/recover
及 error propagation 時依 LIFO 執行。ARC、str、List、closure、class、struct、
Drop 與 defer 必須共用 cleanup planner。

## Error 與並行

錯誤語法保留 `throws Error`、`stop`、`catch err`、`recover value`。recover value
必須符合原 expression 型別；void expression 使用無值 `recover`。`Error` 包含
message、source file、line、column 與 function stack frames。未處理錯誤以紅字
輸出，且不能略過既定 cleanup。destructor 或 deferred call 不可再產生 throws
路徑。

`spawn call()` 建立 OS-backed `Task<T>`，`await task` 恰好消耗一次。Atomic ARC
只保證 ownership count 的跨執行緒安全，不會自動讓 captured mutable payload
免除 data race。

## 編譯器架構

規範 pipeline：

```text
Lexer/Parser
-> Module Graph + SymbolPath Resolver
-> Typed AST/IR
-> Generic Validation + Monomorphization
-> Borrow/Ownership Analysis
-> Cleanup Planning
-> C Backend
-> Artifact Builder
```

checker 與 backend 必須使用同一份 typed expression 結果；Typed IR 不能把 C
source fragment 當成語意表示，以保留未來 LLVM backend 的可能性。

## 安全邊界

Atomic ARC 防止一般 owned value 被提早釋放，但不處理 cycle。區域 borrow system
刻意小於 Rust 的完整 lifetime system。Native C 與 unsafe compatibility wrapper
可破壞所有語言保證，必須視為應用程式的 trusted code。
